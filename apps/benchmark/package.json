{
  "name": "benchmark",
  "version": "0.1.0",
  "private": true,
  "license": "UNLICENSED",
  "scripts": {
    "dev": "playwright test --ui --ui-port 8001",
    "bench": "node scripts/benchmark.js",
    "bench:pw": "playwright test --repeat-each 100 --workers 1",
    "format": "prettier --write \"{tests,utils}/**/*.{js,ts,json}\""
  },
  "bin": {
    "bechmark": "./scripts/benchmark.js"
  },
  "prettier": "@bsc-web-benchmark/prettier-config",
  "devDependencies": {
    "@bsc-web-benchmark/prettier-config": "workspace:*",
    "@playwright/test": "^1.45.2",
    "@types/node": "20.12.7"
  },
  "dependencies": {
    "commander": "^12.1.0"
  }
}
