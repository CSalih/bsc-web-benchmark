{
  "name": "app-leptos",
  "version": "0.1.0",
  "private": true,
  "license": "UNLICENSED",
  "scripts": {
    "dev": "trunk serve --port 3001",
    "build": "trunk build --release",
    "build:docker": "cp ../../Cargo.lock . && docker build -t bsc/app-leptos:latest ."
  }
}
