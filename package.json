{
  "name": "bsc-web-benchmark",
  "version": "0.1.0",
  "license": "UNLICENSED",
  "engines": {
    "node": "^18",
    "pnpm": ">=9"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "pnpm --stream --parallel --filter \"...{apps/**}\" build"
  }
}
