{
  "name": "bsc-web-benchmark",
  "version": "0.1.0",
  "license": "UNLICENSED",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "pnpm --stream --parallel --filter \"...{apps/**}\" build"
  }
}
